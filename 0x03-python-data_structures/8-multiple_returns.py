#!/usr/bin/python3


def multiple_returns(sentence):
    if len(sentence) == 0:
        sentence[i] = None
    i = 0
    length = len(sentence)
    tuple_a = (length, sentence[i])

    return tuple_a
